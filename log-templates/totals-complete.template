<% if(_.size(additionsByFile) && _.size(coverageByFile)){ %>Total diff coverage:

 - lines: ${displayCoverage(totals, 'lines')}
 - branches: ${displayCoverage(totals, 'branches')}
 - functions: ${displayCoverage(totals, 'functions')}
<% } %>